% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_weighted_umap.R
\name{RunWeightedUMAP}
\alias{RunWeightedUMAP}
\title{Run Variance-Weighted UMAP on a Seurat Object}
\usage{
RunWeightedUMAP(
  object,
  reduction = "pca",
  dims = NULL,
  weight.by = c("prop.var", "stdev", "mp", "none"),
  graph = NULL,
  reduction.name = "wt.umap",
  reduction.key = "wtUMAP_",
  n.neighbors = 30L,
  n.components = 2L,
  metric = "euclidean",
  min.dist = 0.3,
  spread = 1,
  seed.use = 42L,
  weight.factor = 1,
  log.scale = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A Seurat object with a PCA (or other linear) reduction
already computed.}

\item{reduction}{Name of the dimensional reduction to use as input.
Default: \code{"pca"}.}

\item{dims}{Integer vector of dimensions to use. If \code{NULL} (default), all
available dimensions are used and a warning is raised when there are more
than 50.}

\item{weight.by}{Weighting scheme applied to PC scores before UMAP.
One of:
\describe{
\item{\code{"prop.var"}}{Proportion of variance explained
(\code{sdev^2 / sum(sdev^2)}). Default.}
\item{\code{"stdev"}}{Standard deviation, normalised (\code{sdev / sum(sdev)}).}
\item{\code{"none"}}{No weighting; equivalent to standard UMAP on PCA
scores.}
}}

\item{graph}{Optional name of a precomputed Seurat KNN graph produced by
\code{\link[=RunWeightedNeighbors]{RunWeightedNeighbors()}} (e.g. \code{"wt_nn"}).  When supplied, UMAP is run
on the weighted PC embedding that generated the graph (stored by
\code{RunWeightedNeighbors()} as \code{"<prefix>.pca"}, where \verb{<prefix>} is derived
by stripping the \verb{_nn} suffix from the graph name).  This ensures the
UMAP topology is computed in exactly the same weighted space as the
clustering graphs.  The \code{dims}, \code{weight.by}, \code{reduction}, and \code{metric}
arguments are ignored when \code{graph} is supplied.  Set \code{n.neighbors} to
match the \code{k.param} used in \code{\link[=RunWeightedNeighbors]{RunWeightedNeighbors()}}.  Default: \code{NULL}.}

\item{reduction.name}{Name under which to store the new UMAP reduction in
\code{object}. Default: \code{"wt.umap"}.}

\item{reduction.key}{Column‐name prefix for the UMAP dimensions, e.g.
\code{"wtUMAP_"} produces column names \code{wtUMAP_1}, \code{wtUMAP_2}, …}

\item{n.neighbors}{Number of nearest neighbours used by UMAP. Default: \code{30}.}

\item{n.components}{Number of UMAP dimensions to compute. Default: \code{2}.}

\item{metric}{Distance metric passed to \code{\link[uwot:umap]{uwot::umap()}}. Default:
\code{"euclidean"}.}

\item{min.dist}{Effective minimum distance between embedded points.
Default: \code{0.3}.}

\item{spread}{Effective scale of embedded points. Default: \code{1}.}

\item{seed.use}{Random seed for reproducibility. Set to \code{NULL} to skip
\code{set.seed()}. Default: \code{42}.}

\item{weight.factor}{A number in \verb{[0, 1]} that scales the influence of the
chosen weighting scheme.  At \code{1} (default) the full variance-derived
weights are applied.  At \code{0} all PCs contribute equally, equivalent to
\code{weight.by = "none"} (standard UMAP).  Intermediate values blend
uniformly-weighted and fully-weighted embeddings.}

\item{log.scale}{Logical. If \code{TRUE}, \code{log1p()} is applied to the weights
before the \code{weight.factor} interpolation, compressing the dynamic range
so that intermediate PCs receive relatively more influence.  Ignored
when \code{weight.by = "none"}.  Default: \code{FALSE}.}

\item{verbose}{Print progress messages and per-PC weights? Default: \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{\link[uwot:umap]{uwot::umap()}}.}
}
\value{
The input Seurat \code{object} with a new \code{DimReduc} named
\code{reduction.name} appended. The \code{misc} slot of this reduction stores:
\itemize{
\item \code{weight.by} — chosen weighting scheme
\item \code{weights}    — numeric vector of weights (one per dimension used)
\item \code{source.reduction} — name of the source reduction
\item \code{dims.used} — integer vector of dimensions used
}
}
\description{
Scales each principal component embedding by a weight derived from the
variance explained by that component, then runs UMAP via \pkg{uwot}.
The result is stored as a new \code{DimReduc} in the returned Seurat object.
}
\examples{
\dontrun{
library(Seurat)
library(wUMAP)
library(patchwork)

# Assumes pbmc has PCA already run

# Standard UMAP — all PCs weighted equally
pbmc <- RunWeightedUMAP(pbmc, dims = 1:30, weight.by = "none",
                        reduction.name = "umap.std")

# Weighted UMAP — PCs scaled by proportion of variance explained
pbmc <- RunWeightedUMAP(pbmc, dims = 1:30, weight.by = "prop.var",
                        reduction.name = "wt.umap")

# Log-scaled weights — compresses dynamic range so intermediate PCs
# contribute more relative to the dominant PCs
pbmc <- RunWeightedUMAP(pbmc, dims = 1:30, weight.by = "prop.var",
                        log.scale = TRUE, reduction.name = "wt.umap.log")

# Standard deviation weighting
pbmc <- RunWeightedUMAP(pbmc, dims = 1:30, weight.by = "stdev",
                        reduction.name = "wt.umap.sd")

# Compare standard vs weighted side by side
p1 <- DimPlot(pbmc, reduction = "umap.std",   label = TRUE) +
  ggtitle("Standard UMAP")
p2 <- DimPlot(pbmc, reduction = "wt.umap",    label = TRUE) +
  ggtitle("Weighted UMAP (prop.var)")
p3 <- DimPlot(pbmc, reduction = "wt.umap.log", label = TRUE) +
  ggtitle("Weighted UMAP (prop.var, log.scale)")
p1 | p2
}
}
